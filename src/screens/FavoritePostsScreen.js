import React from 'react';
import {Text, View} from 'react-native';
import {styles} from "../../styles";

export const FavoritePostsScreen = ({}) => {

    return (
        <View style={styles.container}>
            <Text>FavoritePostsScreen</Text>
        </View>
    );
}
